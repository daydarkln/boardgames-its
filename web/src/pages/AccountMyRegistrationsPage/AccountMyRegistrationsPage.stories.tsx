import type { Meta, StoryObj } from '@storybook/react'

import AccountMyRegistrationsPage from './AccountMyRegistrationsPage'

const meta: Meta<typeof AccountMyRegistrationsPage> = {
  component: AccountMyRegistrationsPage,
}

export default meta

type Story = StoryObj<typeof AccountMyRegistrationsPage>

export const Primary: Story = {}
